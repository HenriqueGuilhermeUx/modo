name: Debug content engine typecheck

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  debug:
    if: github.head_ref == 'agent/content-engine-production-v1'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/content-engine-production-v1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --include=dev
      - name: Capture typecheck
        shell: bash
        run: |
          set +e
          npm run typecheck > content-engine-typecheck.log 2>&1
          code=$?
          echo "EXIT_CODE=$code" >> content-engine-typecheck.log
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: content-engine-typecheck
          path: content-engine-typecheck.log
          if-no-files-found: error
