import type{FastifyInstance,FastifyRequest}from"fastify";import type{AuthService}from"../services/auth-service.js";import{AuthError}from"../services/auth-service.js";import type{MediaConnectionService,MediaProvider}from"../services/media-connection-service.js";function token(r:FastifyRequest){const v=r.headers.authorization;if(!v?.startsWith("Bearer "))throw new AuthError("UNAUTHORIZED",401,"Faça login para continuar.");return v.slice(7).trim()}export async function registerMediaConnectionRoutes(app:FastifyInstance,o:{auth:AuthService;media:MediaConnectionService}){const ctx=(r:FastifyRequest)=>o.auth.authenticate(token(r));app.get("/api/v1/media-connections/health",async()=>({status:"ok",storage:o.media.storage,providers:["google_ads","meta_ads"],capabilities:["connection-registry","oauth-ready-boundary","authorized-account-attach"]}));app.get("/api/v1/media-connections",async r=>o.media.list(await ctx(r)));app.post("/api/v1/media-connections/:provider/prepare",async(r,reply)=>{try{return reply.code(201).send(await o.media.prepare(await ctx(r),(r.params as any).provider as MediaProvider))}catch(e){return reply.code(400).send({error:"MEDIA_CONNECTION_ERROR",message:e instanceof Error?e.message:"Não foi possível preparar a conexão."})}});app.post("/api/v1/media-connections/:id/authorized",async(r,reply)=>{try{return await o.media.attachAuthorizedAccount(await ctx(r),(r.params as any).id,r.body)}catch(e){return reply.code(400).send({error:"MEDIA_CONNECTION_ERROR",message:e instanceof Error?e.message:"Não foi possível concluir a conexão."})}})}