# Prospector next steps

1. Configure `APIFY_TOKEN` and `APIFY_PROSPECTOR_ACTOR_ID` in production.
2. Validate normalized actor output against real campaigns.
3. Add optional provider adapters for enrichment and email verification.
4. Add approved-send integrations only after explicit user approval and channel compliance checks.
5. Feed replies and conversion outcomes back into MODO Signal.